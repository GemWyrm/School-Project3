import React from "react"; // import react in each file

// Page component
function About() {
  return <p>Home Page Content</p>;
}

// export page compoent just like any other component so it can be used elsewhere via 'import'
export default About;
